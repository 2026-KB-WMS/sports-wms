<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Product Page</title>
    <style>
        .form-group { margin-bottom: 10px; }
        .error { color: red; font-size: 0.9em; }
        .option-group { margin-bottom: 5px; }
    </style>
</head>
<body>
<h1>Product Management</h1>
<hr>

<h2>신규 상품 및 SKU 등록</h2>
<form action="/product" method="post">
    <div class="form-group">
        <label for="categoryId">카테고리:</label>
        <select id="categoryId" name="categoryId" required>
            <option value="">카테고리 선택</option>
            {{#categories}}
                <option value="{{id}}">{{name}}</option>
            {{/categories}}
        </select>
    </div>

    <div class="form-group">
        <label for="productName">상품명:</label>
        <input type="text" id="productName" name="productName" required>
    </div>

    <div class="form-group">
        <label for="brand">브랜드명:</label>
        <input type="text" id="brand" name="brand" required>
    </div>

    <div class="form-group">
        <label for="price">가격:</label>
        <input type="number" id="price" name="price" value="0" min="0" required>
    </div>

    <div class="form-group">
        <label for="skuName">SKU 명:</label>
        <input type="text" id="skuName" name="skuName" required>
    </div>

    <div class="form-group">
        <label for="skuCode">SKU 코드:</label>
        <input type="text" id="skuCode" name="skuCode" required>
    </div>

    <div class="form-group">
        <label>옵션 선택 (최소 1개 이상):</label><br>
        {{#optionGroups}}
            <div class="option-group">
                <strong>{{name}}:</strong>
                {{#optionValues}}
                    <label>
                        <input type="checkbox" name="optionValueIds" value="{{id}}">
                        {{name}}
                    </label>
                {{/optionValues}}
            </div>
        {{/optionGroups}}
    </div>

    <button type="submit">등록</button>
</form>

<hr>

<h2>등록된 SKU 목록</h2>
<table border="1" cellpadding="5" cellspacing="0">
    <thead>
    <tr>
        <th>카테고리</th>
        <th>브랜드</th>
        <th>상품명</th>
        <th>SKU 명</th>
        <th>SKU 코드</th>
        <th>옵션</th>
        <th>시스템 등록 가격</th>
    </tr>
    </thead>
    <tbody>
    {{#skus}}
        <tr>
            <td>{{product.category.name}}</td>
            <td>{{product.brand}}</td>
            <td>{{product.name}}</td>
            <td>{{name}}</td>
            <td><strong>{{skuCode}}</strong></td>
            <td>
                {{#optionSKUMappings}}
                    [{{optionValue.optionGroup.name}} : {{optionValue.name}}]<br>
                {{/optionSKUMappings}}
            </td>
            <td>{{product.price}}원</td>
        </tr>
    {{/skus}}

    {{^skus}}
        <tr>
            <td colspan="7" style="text-align: center;">현재 시스템에 등록된 물류 SKU 마스터 데이터가 없습니다.</td>
        </tr>
    {{/skus}}
    </tbody>
</table>

</body>
</html>
